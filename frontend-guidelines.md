# 共用前端設計規範

這份文件用來放 RonFlow 前端跨畫面共用的設計規範。它不是某一個畫面的 feature spec，而是多個畫面都應遵守的共同互動原則。

## 1. 表單提交

### 1.1 避免表單重複提交

所有會建立、更新或刪除資料的表單，都應遵守以下規則：

```text
1. 使用者送出表單後，在同一筆請求完成前，系統應阻止再次送出同一筆表單。
2. 阻止重複提交的方式可以是停用 submit button、忽略後續點擊，或其他等效做法；但結果必須是同一份表單資料不會因為連點而送出多次。
3. 若送出成功，畫面應立即進入成功後的下一個明確狀態，例如關閉 Modal、導頁、更新列表或更新 detail。
4. 若送出失敗，表單應維持可修正狀態，且使用者已輸入的資料不應被清空，除非該欄位內容已被系統判定不可保留。
5. 若表單有欄位驗證錯誤，錯誤訊息應顯示在對應欄位附近，而不是只顯示在畫面其他位置。
```

## 2. 非同步資料讀取

### 2.1 Loading State

所有透過非同步請求讀取資料的畫面或區塊，都應遵守以下規則：

```text
1. 在資料尚未載入完成時，系統應顯示明確的 loading state。
2. Loading state 的作用範圍應與資料讀取範圍一致；整頁資料讀取使用 page-level loading，局部區塊資料讀取使用該區塊自己的 loading state。
3. 在 loading 期間，畫面不應把尚未載入完成的內容誤顯示為 empty state 或成功載入完成的內容。
```

### 2.2 Error State

所有透過非同步請求讀取資料的畫面或區塊，都應遵守以下規則：

```text
1. 當資料讀取失敗時，系統應顯示明確的 error state，且至少包含可見的失敗訊息。
2. Error state 的作用範圍應與失敗範圍一致；整頁資料讀取失敗使用 page-level error，局部區塊資料讀取失敗使用該區塊自己的 error state。
3. 讀取失敗時，畫面不應把失敗結果誤顯示為 empty state。
4. 若目前畫面內容可能已過期或不完整，系統不應把過期資料偽裝成最新成功載入的內容。
```